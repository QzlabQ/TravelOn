[pytest]
testpaths = integration smoke
markers =
    integration: requires the running TravelOn service stack
    resilience: needs the runner to reconfigure or restart a service (run separately by run_tests.py)
