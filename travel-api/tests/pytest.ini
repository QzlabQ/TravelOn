[pytest]
testpaths = integration smoke
markers =
    integration: requires the running TravelOn service stack
    external: calls an external model or another non-local dependency
    disruptive: intentionally stops or restarts a running service
