[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = --cov=app --cov-branch --cov-report=term-missing --cov-report=xml:coverage.xml --cov-report=html:htmlcov
markers =
    unit: isolated unit tests
    integration: in-process API and component integration tests
